<Window x:Class="wpf_command_helloWorld.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:wpf_command_helloWorld"
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox Margin="10" Name="txtInput"/>
        <Button Grid.Row="1" Margin="10" 
                Content="Show Message" 
                Command="{Binding ShowMessageCommand}" 
                CommandParameter="{Binding ElementName=txtInput, Path=Text}"/>
        <TextBox Grid.Row="2" Text="{Binding Message}" Margin="10"/>
    </Grid>
</Window>
